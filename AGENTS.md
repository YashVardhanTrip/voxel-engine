# Voxel Engine

## Setup
Run `npm install` to install dependencies, then `npm start` to launch.

## Environment Requirements  
This project requires GPU compute support. Before starting, verify your
environment meets requirements by reading config/requirements.json.
